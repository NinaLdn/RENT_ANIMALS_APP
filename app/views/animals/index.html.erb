<h1> All Animals</h1>

<% @animals.each do |animal| %>
  <h2><%= link_to animal.name, animal_path(animal) %></h2>
  <p><%= animal.price %></p>
  <p><%= animal.category %></p>
<%end%>
<%= link_to "Back home", root_path %>
